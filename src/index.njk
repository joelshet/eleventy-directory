---
title: Coolest directory site
layout: layout.njk
---

<div class="cool">
<h1>The Coolest <span class="accent">Directory</span> Site on the Internet</h1>
<p>Click stuff and scroll and things and be going wow!</p>
</div>

<section>
<h2><span class="accent">Directory</span>.Map</h2>
<div id="map"></div>
</section>

<section>
<h2><span class="accent">Directory</span>.Search</h2>
<input
  type="search"
  name="q"
  placeholder="Search for an item... (Press '/' to focus)"
  hx-post="/.netlify/functions/search"
  hx-trigger="keyup changed delay:300ms"
  hx-target="#directory-results"
/>

<h2><span class="accent">Directory</span>.Items</h2>
<div id="directory-results" class="directory-grid">
  {% for item in directory %}
    {% include "item-card.njk" %}
  {% endfor %}
</div>
</section>

<h2>Directory</span>.Index</h2>
<div class="index-grid">
{% for item in directory %}
<div>
    {{ item.name }} - {{ item.description }}
    </div>
  {% endfor %}
</div>

{# Pass location data to our client-side map script #}
<script>
  const locations = {{ directory | dump | safe }};
</script>
<script src="/js/map.js" defer></script> 